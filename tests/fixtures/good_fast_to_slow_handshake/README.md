# Fixture: `good_fast_to_slow_handshake`

<!-- generator: scripts/gen_fixture_docs.py -->

Positive counterpart to bad_fast_to_slow_control_loss (CDC-013).

Same fast-to-slow clock ratio (src_clk 2.0ns, dst_clk 20.0ns), and the same goal of forwarding a single event from the fast domain to the slow domain. Where the bad fixture toggles a level and risks losing event accounting when two events occur between dst samples, this one holds the request until the destination acknowledges it (synced back through a 2FF chain). The source's enable that re-arms a new request waits for an ack, so a second event cannot be issued before the first has been observed.

CDC-013 must NOT fire — the src request flop's D is a priority- encoded $mux nest (one branch sets on req_in, another clears on synced ack, the default holds), not the Q/~Q toggle shape the classifier matches.

Two async crossings (the synced-back ack and the held request).

- **Status:** positive — textbook fix; analyzer must stay silent
- **Top module:** `good_fast_to_slow_handshake`
- **Clocks:** `dst_clk` (20.0 ns), `src_clk` (2.0 ns)
- **Crossings:** 2 structural (2 async-per-SDC, 0 sync)

## Clock-domain map

```mermaid
%% good_fast_to_slow_handshake — generated by rtl-buddy-cdc render
flowchart LR
  subgraph clk_dst_clk["dst_clk · 20.0 ns"]
    direction TB
    f_4d44bbad["$procdff$18<br/><i>sv:54</i>"]:::ckcls_dst_clk
    f_e9212816["$procdff$23<br/><i>sv:54</i>"]:::ckcls_dst_clk
    f_739d4e6b["$procdff$28<br/><i>sv:54</i>"]:::ckcls_dst_clk
    f_3b8f8ba3["$procdff$33<br/><i>sv:54</i>"]:::ckcls_dst_clk
    f_02840d86["$procdff$38<br/><i>sv:54</i>"]:::ckcls_dst_clk
  end
  subgraph clk_src_clk["src_clk · 2.0 ns"]
    direction TB
    p_in_event_in[/"event_in⟨in⟩"/]:::ckcls_src_clk
    f_b81cb8f4["$procdff$43<br/><i>sv:45</i>"]:::ckcls_src_clk
    f_3f1881b3["$procdff$48<br/><i>sv:31</i>"]:::ckcls_src_clk
    f_432592af["$procdff$53<br/><i>sv:31</i>"]:::ckcls_src_clk
  end
  f_e9212816 -. "⚠ async · 1b" .-> f_3f1881b3
  f_b81cb8f4 -. "⚠ async · 1b" .-> f_739d4e6b
  subgraph rb_legend["Legend"]
    direction LR
    lg_clk_dst_clk["flop · dst_clk (20.0 ns)"]:::ckcls_dst_clk
    lg_clk_src_clk["flop · src_clk (2.0 ns)"]:::ckcls_src_clk
    lg_port[/"port (no clock)"/]:::port_unassigned
    lg_sync_a[" "]:::ckcls_legend_neutral
    lg_sync_b[" "]:::ckcls_legend_neutral
    lg_async_a[" "]:::ckcls_legend_neutral
    lg_async_b[" "]:::ckcls_legend_neutral
    lg_sync_a --- |"sync crossing"| lg_sync_b
    lg_async_a -. "⚠ async crossing" .-> lg_async_b
  end
  style clk_dst_clk fill:none,stroke:#cbd5e1
  style clk_src_clk fill:none,stroke:#cbd5e1
  style rb_legend fill:none,stroke:#cbd5e1
  style lg_clk_dst_clk font-size:11px
  style lg_clk_src_clk font-size:11px
  style lg_port font-size:11px
  style lg_sync_a font-size:11px
  style lg_sync_b font-size:11px
  style lg_async_a font-size:11px
  style lg_async_b font-size:11px
  classDef ckcls_dst_clk fill:#e0f2fe,stroke:#0369a1,stroke-width:1.5px,color:#0f172a
  classDef ckcls_src_clk fill:#fef3c7,stroke:#b45309,stroke-width:1.5px,color:#0f172a
  classDef port_unassigned fill:#f4f4f5,stroke:#71717a,color:#0f172a
  classDef ckcls_legend_neutral fill:#ffffff,stroke:#cbd5e1,stroke-width:1px,color:#0f172a
```

## Files

- `good_fast_to_slow_handshake.json`
- `good_fast_to_slow_handshake.sdc`
- `good_fast_to_slow_handshake.sv`

---
*Generated by `scripts/gen_fixture_docs.py` — re-run after editing the SV/SDC/netlist.*
