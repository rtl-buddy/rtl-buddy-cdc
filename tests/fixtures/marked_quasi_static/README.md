# Fixture: `marked_quasi_static`

<!-- generator: scripts/gen_fixture_docs.py -->

Positive-case fixture for the `(* cdc_static *)` attribute (issue #173). Configuration / mode bits are programmed once at boot and then held constant during the operating window; they are structurally cross-domain crossings without a synchroniser, but the metastability failure mode CDC-001 / CDC-004 target cannot occur because the value is not transitioning.

Both the 1-bit `cfg_mode_q` and the 4-bit `cfg_data_q` registers are annotated `(* cdc_static *)`. The dst-domain consumer (`obs_q`) samples them on `dst_clk` with NO sync chain — a depth-1 1-bit consumer would normally trip CDC-001, and a 4-bit consumer without gating / gray-coding would normally trip CDC-004. The attribute on the source flops suppresses both rules.

Paired with `bad_quasi_static_unmarked` (same RTL, no attribute): removing the annotation makes CDC-001 and CDC-004 fire as expected.

- **Status:** marker — exercises an SV attribute (`cdc_sync` / `cdc_gray` / …)
- **Top module:** `marked_quasi_static`
- **Clocks:** `cfg_clk` (20.0 ns), `dst_clk` (7.5 ns)
- **Crossings:** 2 structural (2 async-per-SDC, 0 sync)

## Clock-domain map

```mermaid
%% marked_quasi_static — generated by rtl-buddy-cdc render
flowchart LR
  subgraph clk_cfg_clk["cfg_clk · 20.0 ns"]
    direction TB
    p_in_cfg_data_in[/"cfg_data_in⟨in⟩"/]:::ckcls_cfg_clk
    p_in_cfg_mode_in[/"cfg_mode_in⟨in⟩"/]:::ckcls_cfg_clk
    p_in_cfg_we[/"cfg_we⟨in⟩"/]:::ckcls_cfg_clk
    f_3dcaac41["$procdff$23<br/><i>sv:35</i>"]:::ckcls_cfg_clk
    f_1188c53d["$procdff$28<br/><i>sv:35</i>"]:::ckcls_cfg_clk
  end
  subgraph clk_dst_clk["dst_clk · 7.5 ns"]
    direction TB
    f_894c988c["$procdff$13<br/><i>sv:51</i>"]:::ckcls_dst_clk
    f_8794f0ed["$procdff$18<br/><i>sv:51</i>"]:::ckcls_dst_clk
  end
  f_3dcaac41 -. "⚠ async · 1b" .-> f_894c988c
  f_1188c53d -. "⚠ async · 4b" .-> f_8794f0ed
  subgraph rb_legend["Legend"]
    direction LR
    lg_clk_cfg_clk["flop · cfg_clk (20.0 ns)"]:::ckcls_cfg_clk
    lg_clk_dst_clk["flop · dst_clk (7.5 ns)"]:::ckcls_dst_clk
    lg_port[/"port (no clock)"/]:::port_unassigned
    lg_sync_a[" "]:::ckcls_legend_neutral
    lg_sync_b[" "]:::ckcls_legend_neutral
    lg_async_a[" "]:::ckcls_legend_neutral
    lg_async_b[" "]:::ckcls_legend_neutral
    lg_sync_a --- |"sync crossing"| lg_sync_b
    lg_async_a -. "⚠ async crossing" .-> lg_async_b
  end
  style clk_cfg_clk fill:none,stroke:#cbd5e1
  style clk_dst_clk fill:none,stroke:#cbd5e1
  style rb_legend fill:none,stroke:#cbd5e1
  style lg_clk_cfg_clk font-size:11px
  style lg_clk_dst_clk font-size:11px
  style lg_port font-size:11px
  style lg_sync_a font-size:11px
  style lg_sync_b font-size:11px
  style lg_async_a font-size:11px
  style lg_async_b font-size:11px
  classDef ckcls_cfg_clk fill:#e0f2fe,stroke:#0369a1,stroke-width:1.5px,color:#0f172a
  classDef ckcls_dst_clk fill:#fef3c7,stroke:#b45309,stroke-width:1.5px,color:#0f172a
  classDef port_unassigned fill:#f4f4f5,stroke:#71717a,color:#0f172a
  classDef ckcls_legend_neutral fill:#ffffff,stroke:#cbd5e1,stroke-width:1px,color:#0f172a
```

## Files

- `marked_quasi_static.json`
- `marked_quasi_static.sdc`
- `marked_quasi_static.sv`

---
*Generated by `scripts/gen_fixture_docs.py` — re-run after editing the SV/SDC/netlist.*
